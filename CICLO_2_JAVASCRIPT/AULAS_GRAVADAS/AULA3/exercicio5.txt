1. Dê um exemplo de uma variável obedecendo a boa prática camelCase.
    RESP.: var nomeDoUsuario
2. Por padrão, qual o tipo de dado atribuido a uma variável quando esta não
    recebe nenhum valor?
    RESP.: o JS atribui um tipo underfined
3. Quais são as duas categorias de tipos de dados do JS?
    RESP.: o tipo primitivos e não primitivos
4. É verdade que o JS é uma linguagem server-side? Explique.
    RESP.: Não é verdade, pois o JS é uma linguagem client-side, porque ele roda
         no computador do cliente, ou seja roda localmente, já a linguagem server-side
         roda no servidor.